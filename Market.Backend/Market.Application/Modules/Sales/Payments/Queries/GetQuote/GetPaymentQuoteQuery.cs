namespace Market.Application.Modules.Sales.Payments.Queries.GetQuote
{
    // What the signed-in person's cart will actually be charged, priced by the server.
    public sealed class GetPaymentQuoteQuery : IRequest<GetPaymentQuoteQueryDto>
    {
    }
}
